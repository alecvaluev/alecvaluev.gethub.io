import{_ as e,o as t,c as a,f as o}from"./app.97d9f3fc.js";const i="/images/cs/graph3.png",r={},s=o('<h1 id="graphs" tabindex="-1"><a class="header-anchor" href="#graphs" aria-hidden="true">#</a> Graphs</h1><p>A <code>graph</code> is made up of a set of vertices and edges that form connections between vertices. If the edges are directed, the graph is sometimes called a <code>digraph</code>.</p><h2 id="terminology" tabindex="-1"><a class="header-anchor" href="#terminology" aria-hidden="true">#</a> Terminology</h2><ul><li><code>adjacent</code> - Given two nodes A and B. B is adjacent to A if there is a connection from A to B. In a digraph if B is adjacent to A, it doesn&#39;t mean that A is automatically adjacent to B.</li><li><code>edge weight</code>/<code>edge cost</code> - a value associated with a connection between two nodes</li><li><code>path</code> - a ordered sequence of vertices where a connection must exist between consecutive pairs in the sequence.</li><li><code>simplepath</code> - every vertex in path is distinct</li><li><code>pathlength</code> - number of edges in a path</li><li><code>cycle</code> - a path where the starting and ending node is the same</li><li><code>strongly connected</code> - If there exists some path from every vertex to every other vertex, the graph is strongly connected.</li><li><code>weakly connect</code> - if we take away the direction of the edges and there exists a path from every node to every other node, the digraph is weakly connected.</li></ul><h2 id="rrepresentation" tabindex="-1"><a class="header-anchor" href="#rrepresentation" aria-hidden="true">#</a> Rrepresentation</h2><ol><li>Adjacency Matrix</li></ol><ul><li>2 dimensional array. Each index value represents a node.</li></ul><ol start="2"><li>Adjacency List</li></ol><ul><li>uses an array of linked lists to represent a graph Each element represents a vertex. For each vertex it is connected to, a node is added to it&#39;s linked list. For graphs with weights each node also stores the weight of the connection to the node. Adjacency lists are much better if the graph is sparse.</li></ul><p><img src="'+i+'" alt="graph"></p><h2 id="dijkstra-s-algorithm" tabindex="-1"><a class="header-anchor" href="#dijkstra-s-algorithm" aria-hidden="true">#</a> Dijkstra&#39;s Algorithm</h2><p><code>Dijkstra&#39;s Algorithm</code> is an algorithm for finding the shortest path from one vertex to every other vertex. This algorithm is an example of a <code>greedy algorithm</code>.</p><p><code>Greedy algorithms</code> are algorithms that find a solution by picking the best solution encountered thus far and expand on the solution.</p><p>The general algorithm can be described as follows:</p><ol><li>Start at the chosen vertex (we&#39;ll call it v1)</li><li>Store the cost to travel to each vertex that can be reached directly from v1.</li><li>Next look at the vertex that is least costly to reach from v1 (we&#39;ll call this vertex v2 for this examples</li><li>For each vertex that is reachable from v2, find the total cost to that vertex starting from v1 (we want the total cost, not just the lowest cost from v2). Update the cost of travel to these vertices if it is lower than the current known cost.</li><li>Pick the next lowest cost and continue.</li></ol>',15),n=[s];function c(h,d){return t(),a("div",null,n)}const p=e(r,[["render",c],["__file","Graphs.html.vue"]]);export{p as default};
